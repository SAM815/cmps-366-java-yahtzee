<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.YahtzeeGame"
        tools:targetApi="31">
        <activity
            android:name=".view.LogActivity"
            android:exported="false" />
        <activity
            android:name=".view.GameOverActivity"
            android:exported="false" />
        <activity
            android:name=".view.GameActivity"
            android:label="Round Start Game"
            android:parentActivityName=".view.MainActivity"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar" />
        <activity
            android:name=".view.FirstPlayerDetermineActivity"
            android:label="Load Game"
            android:parentActivityName=".view.MainActivity"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar" />
        <activity
            android:name=".view.FileLoadActivity"
            android:label="Load Game"
            android:parentActivityName=".view.MainActivity"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar" /> <!-- <activity android:name=".view.DiceActivity" -->
        <!-- android:label="Dice Roll" -->
        <!-- android:parentActivityName=".view.MainActivity" -->
        <!-- android:theme="@style/Theme.AppCompat.Light.DarkActionBar"> -->
        <!-- </activity> -->
        <!-- <activity android:name=".view.FirstPlayerDetermineActivity" -->
        <!-- android:label="Dice Game" -->
        <!-- android:parentActivityName=".view.MainActivity" -->
        <!-- android:theme="@style/Theme.AppCompat.Light.DarkActionBar"> -->
        <!-- </activity> -->
        <activity
            android:name=".view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>